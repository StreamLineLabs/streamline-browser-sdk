{
  "name": "@streamlinelabs/browser-sdk",
  "version": "0.3.0",
  "description": "Streamline browser SDK — produce/consume directly from the browser via WebSocket/WebTransport, with IndexedDB persistence and CRDT sync (M3, Experimental).",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint 'src/**/*.ts'",
    "typecheck": "tsc --noEmit"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/StreamlineLabs/streamline-browser-sdk"
  },
  "keywords": [
    "streamline",
    "kafka",
    "browser",
    "crdt",
    "edge",
    "offline-first"
  ],
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.57.1",
    "fake-indexeddb": "^5.0.2",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
